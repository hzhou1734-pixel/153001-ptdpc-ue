# 2026-09-10 工作日志

## v1.0.8：品牌名统一改为"顾好家管理平台"

- **需求来源**：用户截图反馈侧边栏标题仍显示"迈极炫平台管理端"，要求改为"顾好家管理平台"。
- **根因定位**："迈极炫"在前端代码中无硬编码，标题来自后端 config 接口下发的 `web_name` 字段，展示点有两处：`src/layout/default/components/sidebar/logo.vue:17`（侧边栏标题）和 `src/views/account/login.vue:11`（登录页标题）；浏览器标签页标题走 `src/permission.ts:64` 的 `config.title`（本地 `src/config/index.ts` 默认值）。
- **修改内容**（commit `2424235`，4 files changed）：
  - `src/stores/modules/app.ts`：新增 `BRAND_NAME = '顾好家管理平台'` 常量，`getConfig()` 拿到后端配置后统一覆盖 `data.web_name = BRAND_NAME`（`title` 为空时兜底），侧边栏与登录页一处改、两处生效。
  - `index.html`：初始 `<title>` 从"后台管理系统"改为"顾好家管理平台"（覆盖首屏加载阶段）。
  - `src/config/index.ts`：`title` 默认值同步改为"顾好家管理平台"（覆盖路由 meta 无 title 时的兜底场景）。
- **推送**：GitHub 直连间歇性中断（github.com:443 连接重置/超时约 1 分钟，api.github.com 正常，本地无代理可用），通过探测确认恢复后重推成功：`5704f4a..2424235  main -> main`。
- **待用户操作**：EdgeOne 重新部署后强刷复测——侧边栏、登录页、浏览器标签页标题均应显示"顾好家管理平台"。

## 备注

- 本次推送姿势与 v1.0.7 相同：`TOKEN=$(cat ~/.ghj_publish_token | tr -d '\r\n')` + 内联 PAT + `GIT_TERMINAL_PROMPT=0` + `-c credential.helper=`，遇网络中断探测恢复后重试即可，无需换姿势。
