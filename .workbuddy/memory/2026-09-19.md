# 2026-09-19

## 协作约定：每次修改后右侧打开预览
- 用户要求：每次修改代码后，必须在右侧内置浏览器预览面板打开 http://localhost:5173 核对效果再交付。
- 已写入项目 MEMORY.md「本地预览」章节作为强约束（dev server 未启动时先 `node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 5173` 拉起）。
- 当前 dev server 探活返回 200，已用 present_files 打开预览核对 v1.0.13（物业列表+小区信息整合）。

## v1.0.14：物业列表「管理小区总数」列改为「小区名称」
- 改动：`src/mock/db.ts` 预生成 `communityNames`（68 项，物业/小区共用，保证名称一致）；`propertyDb` 每项新增 `community_name: communityNames[i]`；`communityDb` 的 `name` 改为引用 `communityNames[i]`（不再独立随机生成）。`src/views/ghj/property/list.vue` 第 124 行列 `label="管理小区总数" prop="community_count"` → `label="小区名称" prop="community_name" min-width=160 show-overflow-tooltip`。
- 原因：一物业一小区后 `community_count` 恒为 1 无意义，改成展示小区名称更直观；共用名称数组避免列表名与小区详情名不一致。
- 提交：`cf61708`（2 files changed, 6 insertions, 2 deletions），本地 `feature/req-align` 已通过 `.git/packed-refs` 前移。vue-tsc 零新增报错（12 条均为 likeadmin 基件既有错误）。
- 推送：当前 GitHub 走 HTTPS 持续 TLS 中断（schannel/openssl 均 "server closed abruptly / unexpected eof"），已启动后台重试循环（task tBO8lR）待网络恢复自动推送；另重建缺失的 `.git/hooks/post-commit`（读取 ~/.ghj_publish_token 内联 PAT 自动推送，main 跳过）。

## v1.0.15：小区信息改为打开小区详情新页面
- 需求：物业列表「小区信息」按钮由弹窗改为打开新页面（详情页），页面含「基础信息」+「订单信息」（托管/膳食/陪诊/生活帮手 4 类订单）。
- 改动文件（5 个，145 ins / 82 del）：
  - `src/config/ghjMenu.ts`：运营管理 children 新增隐藏项 `{ name:'小区详情', paths:'communityDetail', component:'ghj/property/communityDetail', is_show:0 }` → 路由 `/property/communityDetail`（沿用 user/detail 隐藏菜单项范式）。
  - `src/mock/db.ts`：新增 `communityOrderDb(communityId)`（按小区ID确定性生成，4 类订单），`COMMUNITY_ORDER_TYPES`/`COMMUNITY_ORDER_TITLE_MAP`；不影响既有 userOrderDb（仍是 托管/报餐/陪诊）。
  - `src/api/ghj/property.ts`：新增 `getCommunityOrderList({community_id})`。
  - `src/views/ghj/property/communityDetail.vue`：新建详情页，el-tabs（基础信息 el-descriptions + 订单信息 el-table 带类型筛选），`route.query.id` 取参、`router.back()` 返回（镜像 user/detail）。
  - `src/views/ghj/property/list.vue`：移除原小区信息弹窗与 communityRef/getFullAddress/handleCommunity；`handleCommunity` 改为 `router.push({ path:'/property/communityDetail', query:{ id: row.community_id } })`；导入改 useRouter、移除 getCommunityDetail 与 .detail 样式。
- 验证：vue-tsc 零新增报错（12 条均为 likeadmin 基件既有）；提交 `bb9ecd1`，本地 `feature/req-align` 经 `.git/packed-refs` 前移。
- 推送：后台重试循环（task RIGqfL）第 1 次即成功 `8cf3c41..bb9ecd1`，将 v1.0.14+v1.0.15 一并推上 GitHub，本地远端同步。
- 预览提示：新增路由由菜单运行时生成，dev server HMR 可能需整页刷新一次才能在点击「小区信息」时正确进入详情页。

## v1.0.16：用户详情-顾好家币筛选行修复为一行
- 问题（用户截图）：「顾好家币」Tab 中「所属物业」标签被挤成竖排换行、el-select 被拉伸至近整行宽（Element Plus el-select 默认 100% 宽，`w-[280px]` Tailwind 类未生效/被覆盖），「账户余额」被顶到最右侧，整体观感为两行。
- 改动：`src/views/ghj/user/detail.vue` 顾好家币筛选行——标签 span 加 `shrink-0 whitespace-nowrap`；el-select 改为 `style="width: 220px"` + `class="shrink-0"`（内联样式确保生效）；「账户余额」span 由 `ml-6` 改 `ml-auto`（右对齐）+ `shrink-0 whitespace-nowrap`。
- 验证：vue-tsc 仍为 likeadmin 基件既有错误，零新增；提交 `e96dd0a`，packed-refs 前移，推送成功 `bb9ecd1..e96dd0a`。
- 踩坑补充：提交后立即 `rev-parse HEAD` 仍读旧值（refs 子目录写入失败的已知环境问题），必须在同一命令里先取 `rev-parse <short>` 的完整哈希再写 packed-refs，否则会把旧哈希写进去。
- 预览：present_files 打开 http://localhost:5173 核对（用户详情 → 顾好家币 Tab）。

## v1.0.17：用户详情-健康手环精简列
- 需求（用户截图）：健康手环 Tab 删除「心率/血氧/步数/睡眠」四列，新增一条「手机号码」列。
- 改动：`src/views/ghj/user/detail.vue` 健康手环 el-table——移除 heart_rate/blood_oxygen/step/sleep 四列，新增「手机号码」列（模板直接取 `detail.base?.mobile`，即用户基础信息的手机号），保留 手环SN/手机号码/绑定时间 三列。
- 验证：vue-tsc 零新增报错；提交 `69e1219`，packed-refs 前移（注意先 `rev-parse <short>` 再写 refs），推送成功 `e96dd0a..69e1219`。
- 顺带：post-commit 钩子再次缺失，已重建（读 ~/.ghj_publish_token 内联 PAT 推送、main 跳过、fire-and-forget）。
- 预览：present_files 打开 http://localhost:5173 核对（用户详情 → 健康手环 Tab）。

## v1.0.18：员工列表操作列按钮文案精简
- 需求（用户截图）：员工列表（`src/views/ghj/user/staff.vue`）操作列的「员工详情」按钮改为「详情」。
- 改动：仅第 83 行按钮文案 `员工详情` → `详情`（弹窗标题保留「员工详情」）。
- 状态：纯文案改动零风险；提交 `7a52fcf`，packed-refs 前移，推送成功 `69e1219..7a52fcf`。
- 踩坑复现：同一命令内 `rev-parse --short HEAD` 仍返回旧值（refs 写入失败），必须用 `rev-parse <新短哈希>` 二次取完整哈希再写 packed-refs。
- post-commit 钩子再次报缺失，已重建（内容同前）。
- 预览：present_files 打开 http://localhost:5173 核对（员工管理 → 员工列表）。

## v1.0.19：员工列表账号状态改为启用/禁用开关
- 需求：员工列表「账号状态」列由 el-tag 文案改为可交互的启用/禁用开关（el-switch）。
- 改动：
  - `src/api/ghj/user.ts`：新增 `staffStatus({id, status})`，从 staffDb 找到记录并改写 `status`（镜像既有 `userStatus`）。
  - `src/views/ghj/user/staff.vue`：账号状态列 el-tag → `el-switch`（`:active-value="1" :inactive-value="0" @change="handleStatusChange(row)"`）；import `staffStatus` + `ElMessage, ElMessageBox`；新增 `handleStatusChange`——弹确认框（启用/禁用），确认则调 `staffStatus` 并提示成功，取消则 `row.status = oldStatus` 回滚开关。顶部「账号状态」筛选下拉（启用=1/禁用=0）保留不变。
  - 注：页面顶部 alert 仍声明「平台仅查看」，但用户明确要求开关，故做成可交互（MOCK 内即时生效）。
- 验证：vue-tsc 零新增报错（13 条均为 likeadmin 基件既有）；提交 `c6b5d2c`，packed-refs 前移到该提交。
- 推送：直推又撞 schannel `server closed abruptly`，已启动后台重试循环（task a8clmd，openssl sslBackend）待网络恢复自动推送；post-commit 钩子再次缺失，已重建（内容同前）。
- 预览：present_files 打开 http://localhost:5173 核对（员工管理 → 员工列表，账号状态列为开关）。

## v1.0.20：人力资源重构为「服务人员技能认证审核中心」
- 需求（用户）：人力资源模块所有内容按业务场景匹配最贴切的文字与图片，并提升页面排版美观度（人力资源=内容管理下的「技能认证审核」页）。
- 业务场景映射：社区服务人员按服务类目（托管/膳食/陪诊/生活帮手/康复/家政）提交资质认证 → 平台按持证类型（职业资格证书/培训结业证/健康证/身份证明）核验凭证 → 审核通过后纳入人才库接单。
- 改动文件（4 个源码 + 4 张新增 SVG）：
  - `public/mock-img/hr/*.svg`：新增 4 张证书样式凭证图（zyg 职业资格证书/蓝、jy 培训结业证/绿、jk 健康证/青、sf 身份证明/橙），文档版式+印章，与 resource 占位图风格协调。
  - `src/mock/db.ts`：① 顶部新增 `HR_IMGS: Record<string,string>`（按 cert_type 映射 4 张凭证图）；② hrDb 重写——新增 `category`（服务类目，按 HR_CATEGORIES）、`role`（认证角色，按类目映射到真实服务角色 HR_ROLE_BY_CATEGORY）、`cert_type`（持证类型）、`cert_no`（证书编号，按类型前缀 ZY/JY/JK/SF）、`experience_years`（从业年限）、`community_name`（申请人所属社区），credential 按 cert_type 取对应 SVG；保留 skill_title=role 兼容筛选。
  - `src/api/ghj/content.ts`：人力资源接口扩展——`getHrList` 新增 `category` 筛选；新增 `getHrStats()`（按审核状态统计 pending/passed/rejected/total）、`hrAudit({id, audit_status})`（写入 hrDb 状态）。
  - `src/views/ghj/content/hr.vue`：整体重写——页头标题+业务说明；顶部 4 张统计卡（待审核/已通过/已驳回/总认证数，带色块）；筛选栏新增「服务类目」下拉；表格列改为「申请人(头像+昵称+手机)/服务类目(彩色标签)/认证角色/持证类型(标签)/认证凭证(缩略图)/提交时间/审核状态(彩色 tag)/操作」；详情弹窗分区美化（头部类目色块+状态、基础信息、认证信息含证书编号、资质凭证大图预览、底部通过/驳回审核按钮，调用 hrAudit 后刷新列表与统计）。
  - 色彩规范：类目 meta（托管#0CA678/膳食#F76707/陪诊#1C7ED6/生活帮手#E8590C/康复#7048E8/家政#2F9E44），与人才库/小区订单业务线一致。
- 验证：vue-tsc 零新增报错（11 条均为 likeadmin 基件既有：content.ts/sytem.ts/user.ts 基件 + editor/export-data/decoration）；自修 2 处索引类型（HR_IMGS、stats 加 Record 签名）。提交 `7215f0f`，packed-refs 前移（HEAD 已 `rev-parse` 校验=7215f0f、status 干净）。
- 推送：直推成功 `c6b5d2c..7215f0f feature/req-align -> feature/req-align`（含 v1.0.19）。
- 预览：present_files 打开 http://localhost:5173 核对（内容管理 → 人力资源）。

## v1.0.21：人力资源列表移除服务类目，详情同步修改
- 需求（用户）：人力资源列表移除「服务类目」，详情弹窗同步修改。
- 改动（仅 `src/views/ghj/content/hr.vue`，17 ins / 60 del）：
  - 列表：删除「服务类目」筛选下拉（含 `categoryOptions` 常量、queryParams.category）；删除表格「服务类目」彩色标签列。
  - 详情弹窗：① 头部由 `categoryMeta[category]?.bg` 动态色改为固定品牌色 `#e9f7f1`，副标题 `{{category}} · {{role}}` → `{{role}}`；② 认证信息区删除「服务类目」展示项；③ 同步删除 `.cat-tag` 样式与 `categoryMeta` 常量（已无引用）。
  - 页头说明精简：去掉「按服务类目核验」措辞，保留业务一致性。
  - 数据层 hrDb 的 `category` 字段保留（无害），后续如需彻底移除可再清理 db.ts/API。
- 验证：vue-tsc 零新增报错（仍为 likeadmin 基件既有 26 行/约 11 处）；提交 `e17ffbb`，packed-refs 前移（update-ref 在沙箱未生效，直接改 packed-refs）。
- 推送：直推成功 `7215f0f..e17ffbb feature/req-align -> feature/req-align`。
- 预览：present_files 打开 http://localhost:5173 核对（内容管理 → 人力资源）。

## v1.0.22：人才库三项改造
- 需求（用户）：①人才状态开关左右两侧的启用/禁用文字删除；②人才详情补充认证时提交的所有资料；③订单信息加上订单编号以及搜索框。
- 改动（2 个文件，106 ins / 5 del）：
  - `src/mock/db.ts`：① talentDb 扩展认证字段——`category`（取 HR_CATEGORIES）、`role`（按类目取 HR_ROLE_BY_CATEGORY）、`cert_type`（HR_CERT_TYPES）、`cert_no`（按类型前缀 ZY/JY/JK/SF + 编号）、`community_name`（communityNames）、`credential`（HR_IMGS 对应证书 SVG）、`experience_years`、`desc`；② talentOrderDb 订单新增 `order_no`（`GJ` + 12 位随机号）。
  - `src/views/ghj/content/talent.vue`：① 人才状态列 `el-switch` 删除 `active-text`/`inactive-text`（仅保留开关本体）；② 详情弹窗新增「认证资料」Tab，展示认证凭证（image-contain 预览）、认证角色、持证类型、证书编号、所属社区、从业年限、认证描述；③ 订单信息 Tab 顶部新增搜索框（按订单编号/标题/下单用户本地筛选 `filteredOrders` computed），订单表格首列新增「订单编号」列。新增 `.cert-preview`/`.font-mono`/`.order-toolbar`/`.order-search` 样式。
- 验证：vue-tsc 零新增报错（仍为 likeadmin 基件既有 26 行/约 11 处）；提交 `b588002`，packed-refs 前移（rev-parse HEAD 沙箱读旧值，直接改 packed-refs 写 b588002）。
- 推送：直推撞网络抖动（OpenSSL unexpected eof / HTTP2 framing / 502），已启动后台重试循环（task zBlIcy）待恢复自动推送；post-commit 钩子再次缺失（沙箱不持久），未重建自动推送钩子。
- 预览：present_files 打开 http://localhost:5173 核对（内容管理 → 人才库）。

## v1.0.23：人才详情认证资料移除勾选项
- 需求（用户截图红框）：认证资料 Tab 中删除 认证角色/持证类型/证书编号/所属社区/从业年限 五项展示，保留认证凭证与认证描述。
- 改动：仅 `src/views/ghj/content/talent.vue`（24 行删除），并清理无引用的 `.font-mono` 样式；mock 字段保留在 db.ts（无害）。
- 验证：vue-tsc 零新增（仍为基件既有错误）。
- 提交推送：`0e86094` 已推上（`125ec51..0e86094`）。注意：本轮 `git log --format=%h -1` 也读到旧哈希（125ec51），导致 packed-refs 一度被写回旧值——沙箱怪癖升级：commit 后一律用 `rev-parse <commit输出里的短哈希>` 取全哈希，勿用 log 读 HEAD。
- 环境注意：WorkBuddy shim 环境缺 coreutils（cat/tr/dirname 不可用），token 读取必须用 bash 内建 `$(< file)` + 参数扩展去空白；post-commit 钩子已用内建方式重建。

## v1.0.24（250b139，已推送）
- 需求：人才详情「订单信息」的订单评价改为仅评星、无文字。
- 改动：db.ts `talentOrderDb` 用 `comment`→`star`（先 pick status，仅「已完成」生成 1-5 星，其余 0）；talent.vue 订单评价列改 `el-rate` 只读星标，`star` 为 0 显示「-」。
- 提交后钩子再次丢失已重建；packed-refs 按惯例用 commit 输出短哈希取全哈希修正（250b139）。预览端口 5174（平台端，5173 是物业端，勿混）。

## v1.0.25 报名活动详情补充时间字段
- 需求：报名活动详情缺少「活动时间」「报名截止时间」。
- 改动：db.ts `activityDb` 新增 `activity_time: ago(-randInt(10,60),9)`（未来 10-60 天、9 点）、`signup_deadline: ago(-randInt(2,9),18)`（未来 2-9 天、18 点，早于活动时间）；activity.vue 报名详情弹窗在「详情内容」与「创建时间」间新增两项（半宽并排，蓝色高亮）。
- 提交 928e894，已推送 5729981..928e894。preview 端口 5174。
